// Declare a variable without data type
let a; 

// Assigment
a = 5; 

// Define a variable that holds the int8 value   
let b: i8 = 32;

// Define a variable that holds the boolean value   
let f: bool = false; 

// Define two variables x and y, x is 1 and y is 2
let (x, y) = (1, 2); 

// Define a mutable variable, this variable is allowed to be changed.
let mut z = 5;
z = 6;

// Define a constant variable, this variable is not allowed to be changed.
const N: i32 = 5;

// Define a global variable.
static NS: i32 = 5;